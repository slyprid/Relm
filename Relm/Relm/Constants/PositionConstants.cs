using Microsoft.Xna.Framework;
using Relm.States;

namespace Relm.Constants
{
    public static class PositionConstants
    {
        public static Point CenterScreen => new Point(GameCore.ResolutionWidth / 2, GameCore.ResolutionHeight / 2);
    }
}